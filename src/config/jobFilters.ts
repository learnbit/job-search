import type { JobFilters } from "../filters/filterJobs.js";

export const jobFilters: JobFilters = {
  titleKeywords: ["frontend", "front-end", "web developer", "web frontend"],
  skills: ["react", "typescript"],
  workplaces: ["remote", "unknown"],
};
